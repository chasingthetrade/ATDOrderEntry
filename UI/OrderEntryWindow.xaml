<Window x:Class="FIXatdlOrderEntry.UI.OrderEntryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FIXatdl Order Entry" Height="700" Width="900"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderBrush" Value="#2196F3"/>
            <Setter Property="BorderThickness" Value="2"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,5,10,5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,5,0,5"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="MinHeight" Value="25"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="0,5,0,5"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="MinHeight" Value="25"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="0,5,0,5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="DatePicker">
            <Setter Property="Margin" Value="0,5,0,5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="MinHeight" Value="25"/>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Border Grid.Row="0" Background="#1976D2" Padding="15">
            <StackPanel>
                <TextBlock x:Name="TitleTextBlock" Text="FIXatdl Order Entry System" 
                          FontSize="24" FontWeight="Bold" Foreground="White"/>
                <TextBlock x:Name="StrategyNameTextBlock" Text="" 
                          FontSize="14" Foreground="White" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>
        
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="10">
            <StackPanel x:Name="MainPanel">
            </StackPanel>
        </ScrollViewer>
        
        <Border Grid.Row="2" Background="#FFF3E0" Padding="10" Margin="10,0,10,10"
                x:Name="ValidationPanel" Visibility="Collapsed">
            <StackPanel>
                <TextBlock Text="Validation Errors:" FontWeight="Bold" Foreground="#D32F2F" FontSize="14"/>
                <TextBlock x:Name="ValidationErrorsTextBlock" Foreground="#D32F2F" 
                          TextWrapping="Wrap" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>
        
        <Border Grid.Row="3" Background="#F5F5F5" Padding="15" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="SubmitButton" Content="Submit Order" 
                       Width="120" Height="35" Margin="0,0,10,0"
                       Background="#4CAF50" Foreground="White" 
                       FontWeight="Bold" FontSize="14"
                       Click="SubmitButton_Click"/>
                <Button x:Name="ClearButton" Content="Clear" 
                       Width="100" Height="35"
                       Background="#9E9E9E" Foreground="White" 
                       FontWeight="Bold" FontSize="14"
                       Click="ClearButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
